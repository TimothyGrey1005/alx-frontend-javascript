/**
 * Represent classroom.
 */
export default class ClassRoom {
  /**
   * Make new @see {@link ClassRoom}.
   * @param {Number} maxStudentsSize - The max number of students in class
   */
  constructor(maxStudentsSize) {
    this._maxStudentsSize = maxStudentsSize;
  }
}
